# Keep LF in the working tree on every platform. Without this, a Windows checkout with
# core.autocrlf=true gets CRLF on disk, and `gofmt -l .` then reports every file in the
# repo as unformatted while CI stays green.
* text=auto eol=lf

# Never touch these.
*.png binary
*.jpg binary
*.exe binary
